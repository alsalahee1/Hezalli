import { ComingSoon } from "@/components/coming-soon";

// Placeholder — buyer order history is built in Phase 8 (Checkout & orders).
export default function AccountOrdersPage() {
  return <ComingSoon ns="Account" titleKey="orders" />;
}
