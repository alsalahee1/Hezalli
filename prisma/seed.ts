/**
 * Database seed script.
 *
 * This is a placeholder created in Step 2.1. The real seed data
 * (admin user, test sellers/stores, buyers, category tree, sample
 * products and orders) is implemented in Step 2.2 — see
 * docs/02-project-setup.md and docs/DATABASE.md.
 */
async function main() {
  console.log("Seed placeholder — implemented in Step 2.2.");
}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
