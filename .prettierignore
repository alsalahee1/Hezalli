# Dependencies & build output
node_modules
.next
out
build

# Generated / lockfiles
lib/generated
prisma/migrations
package-lock.json
next-env.d.ts

# Hand-authored docs (preserve manual formatting / tables)
*.md

# Tailwind v4 entry stylesheet. prettier-plugin-tailwindcss resolves the design
# system to sort classes, and in CI that resolution is non-deterministic for
# this file — the same commit passes on one run and fails `prettier --check` on
# the next, always flagging globals.css. It can't be reproduced locally. The
# file is hand-authored theme tokens + a couple of @layer/@apply blocks, so we
# skip Prettier here and keep it tidy by hand.
app/globals.css
